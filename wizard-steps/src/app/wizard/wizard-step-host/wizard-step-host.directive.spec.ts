import { WizardStepHostDirective } from './wizard-step-host.directive';

describe('WizardStepHostDirective', () => {
  it('should create an instance', () => {
    const directive = new WizardStepHostDirective();
    expect(directive).toBeTruthy();
  });
});
